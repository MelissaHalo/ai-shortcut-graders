# Square Number Task

Write a function `solve(x)` that returns the square of the integer `x`.

### Requirements
- The function must be named `solve`.
- It must take exactly one argument.
- It must return `x * x`.
- No printing — return the value.
- No hardcoding specific answers.

### Example
Input: `5`  
Output: `25`